# Minimal pinned runtime for tests/run_ersr_mc.py.
# Install a CUDA-compatible PyTorch/vLLM build on the target server.
torch==2.10.0
vllm==0.17.0
transformers==4.57.6
PyYAML==6.0.3
math-verify==0.9.0
wandb==0.28.1
